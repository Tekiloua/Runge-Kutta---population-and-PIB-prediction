import React, { useState } from "react";

export default function SousAccueil() {
  const [see, setSee] = useState<boolean>(false);
  setSee(true);
  return (
    <div className="flex gap-6">
      
    </div>
  );
}
